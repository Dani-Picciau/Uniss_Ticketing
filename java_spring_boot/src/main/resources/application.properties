# Cambia la porta di ascolto del server (di default è 8080)
# server.port=5000

spring.application.name=java_spring_boot

# MongoDB connection
spring.mongodb.uri=mongodb://localhost:27017/Prova
 
# JWT configuration
# Secret key: must be at least 32 characters, keep this private
jwt.secret=uniss-token-web-app-procedure-2026
# Token expiration: 86400000ms = 24 hours
jwt.expiration=86400000