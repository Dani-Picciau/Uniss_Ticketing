import 'package:flutter/material.dart';

class AssignedAdministratorScreen extends StatelessWidget {
  const AssignedAdministratorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Ciao Amninistratore'));
  }
}
