import 'package:flutter/material.dart';

class RequestingProfessorScreen extends StatelessWidget {
  const RequestingProfessorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Ciao Professore'));
  }
}
